(() => {
  'use stricts'

  angular.module('webProject').factory('GroupFactory', [
    // '$http',
    // 'consts',
    GroupFactory
  ])

  function GroupFactory($http, consts) {

    // Requisições com $http
    
  }
})